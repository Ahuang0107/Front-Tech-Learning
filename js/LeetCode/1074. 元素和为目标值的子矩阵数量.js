/**
 * @param {number[][]} matrix
 * @param {number} target
 * @return {number}
 */
var numSubmatrixSumTarget = function(matrix, target) {

};

let test = [];
test.push([[[0,1,0],[1,1,1],[0,1,0]],0]);

for(const value of test){
    console.log(numSubmatrixSumTarget(value[0],value[1]));
}
